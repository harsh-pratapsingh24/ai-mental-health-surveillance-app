flask==3.0.3
authlib==1.3.1
python-dotenv==1.0.1
groq==0.13.0
gunicorn==22.0.0

# Security headers (optional — gracefully degrades if not installed)
flask-talisman==1.1.0

# Database (optional — enables when DATABASE_URL is set)
# flask-sqlalchemy==3.1.1
# alembic==1.14.0
# psycopg2-binary==2.9.10

# Async worker (optional — enables when CELERY_BROKER_URL is set)
# celery==5.4.0
# redis==5.2.1

# Error tracking (optional — enables when SENTRY_DSN is set)
# sentry-sdk[flask]==2.19.2